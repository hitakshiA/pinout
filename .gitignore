node_modules/
.env
.env.*
!.env.example
sessions.jsonl
last-session.json
*.log
.DS_Store
.venv
.compute-spend.json
.agent-sessions/

# Internal engineering record: measurements, build plan, supplier probes.
# Kept out of the public repo, not deleted.
.private/

# scratch scripts written during investigation, never part of the project
.*.mjs
