node_modules/
.env
.env.*
!.env.example
sessions.jsonl
last-session.json
*.log
.DS_Store
.venv
.compute-spend.json
.agent-sessions/

# Internal engineering record: measurements, build plan, supplier probes.
# Kept out of the public repo, not deleted.
.private/

# scratch scripts written during investigation, never part of the project
.*.mjs

# custodial workspace state: holds wallet keys, never commit
.club/
web/node_modules/
web/.next/
.next/
web/.env.local
web/shot.mjs

# design-tool scratch, local to whoever is working on the UI
.impeccable.md

# HyperFrames video projects: multi-GB source footage, renders and
# node_modules. The compositions are small but worthless without the media.
videos/
